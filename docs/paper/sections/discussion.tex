\section{Discussion}
\label{sec:discussion}

\subsection{Relationship Between the Two Approximations}

The two approximations have different theoretical foundations yet share the
same two anchor points: both are exact in the homogeneous case and have the
correct heavy-traffic singularity.

$\Tul$ works by confining the approximation within the known bounds.
Its interpolation weight $\alpha = (\rho_1+\rho_2)/8$ has a natural
interpretation: it is one-quarter of the average utilization, calibrated
to reproduce the Nelson--Tantawi formula at $r = 1$.
By construction, $0 \leq \alpha \leq 1/4$, so $\Tul$ always lies between
the bounds.

$\Tlh$ works from a different direction: it constructs a rational function
that matches the system's known asymptotic behavior at both traffic extremes.
The heavy-traffic factor $h(r) = 1 + \tfrac{3}{8}r^{-\beta}$ captures the
physical transition from a regime where both servers jointly saturate
($r = 1$, $h = 11/8$) to a regime where only the bottleneck server saturates
($r \to \infty$, $h \to 1$).
The sharpness of this transition---$h \approx 1$ already at $r = 1.5$---reflects
the fact that even modest heterogeneity is sufficient to break the joint
saturation and transfer the heavy-load correlation penalty to the bottleneck
alone.

\subsection{Comparison with Prior Work}

Table~\ref{tab:comparison} situates the two new approximations within the
existing literature.

\begin{table}[ht]
\centering
\caption{Comparison of methods for fork-join mean response time ($n = 2$).}
\label{tab:comparison}
\begin{tabular}{@{}lllcc@{}}
\toprule
Reference & Type & Closed-form? & Exact (hom.)? & Heterogeneous? \\
\midrule
Nelson--Tantawi~\citep{Nelson1988} & Approx.\ (hom.\ only) & Yes & Yes & No \\
Flatto--Hahn~\citep{Flatto1984,Flatto1985} & Exact (gen.\ func.) & No & Yes & Yes \\
Baccelli et al.~\citep{Baccelli1989} & Bounds & Partial & No & Yes \\
Varma--Makowski~\citep{Varma1994} & LH interp.\ (hom.) & Yes & Yes & No \\
Kemper--Mandjes~\citep{Kemper2012} & Bounds + asymptotics & Partial & No & Yes \\
Rizk et al.~\citep{Rizk2015} & Distributional bounds & No & No & Yes \\
Mohanty et al.~\citep{Mohanty2024} & Bounds ($(k,k)$ FJ) & Partial & No & Yes \\
\textbf{This work ($\Tul$)} & Bound interpolation & \textbf{Yes} & \textbf{Yes} & \textbf{Yes} \\
\textbf{This work ($\Tlh$)} & LH traffic interp. & \textbf{Yes} & \textbf{Yes} & \textbf{Yes} \\
\bottomrule
\end{tabular}
\end{table}

Varma and Makowski~\citep{Varma1994} applied the Reiman--Simon framework to
the \emph{symmetric} ($\mu_1 = \mu_2$) fork-join queue with $n \geq 2$ servers,
showing that the light-heavy traffic interpolation recovers the exact
Nelson--Tantawi formula for $n = 2$.
Our $\Tlh$ extends this to the heterogeneous case through the $h(r)$ factor,
while $\Tul$ provides an alternative route via direct bound interpolation.

\subsection{Accuracy and Limitations}

$\Tul$ provides consistently small errors ($\leq 1.13\%$) and is
guaranteed to lie within the bounds.
Its main limitation is that the weight $\alpha = (\rho_1+\rho_2)/8$ is
derived from the homogeneous case and may not optimally represent the
correlation structure at intermediate heterogeneity levels.

$\Tlh$ provides a tighter fit near homogeneity but has larger errors ($\leq 1.80\%$)
at the transition regime ($r \approx 1.5$, heavy load), where the linear
numerator has insufficient flexibility to capture the joint dependence on
both $r$ and $\rho$.

$\Tlhe$ improves on $\Tlh$ for $r \geq 2$ by incorporating the first
light-traffic derivative, reducing errors by up to $81\%$ in those scenarios
(from $+0.68\%$ to $+0.13\%$ at $r = 5$).
However, at $r \approx 1.5$ the enhancement is counterproductive: the
quadratic term overshoots, yielding slightly larger errors than $\Tlh$
(worst case $+2.17\%$ vs.\ $+1.80\%$ at heavy load).
The root cause is that $h(r)$ varies steeply near $r = 1$, so any error in
the heavy-traffic anchor is amplified by the higher-order fit.
Both LH variants are therefore less accurate than $\Tul$ in the $r=1.5$ regime.
In all LH variants, the parameter $\beta$ is calibrated to $10$ from simulation
data; an analytic determination (see Section~\ref{sec:conclusion}) could
improve performance uniformly and resolve this limitation.

Also added to Table~\ref{tab:comparison} is $\Tlhe$.

\begin{table}[ht]
\centering
\caption{Updated comparison of methods (replacing Table~\ref{tab:comparison}).}
\label{tab:comparison2}
\begin{tabular}{@{}lllcc@{}}
\toprule
Reference & Type & Closed-form? & Exact (hom.)? & Heterogeneous? \\
\midrule
Nelson--Tantawi~\citep{Nelson1988} & Approx.\ (hom.\ only) & Yes & Yes & No \\
Flatto--Hahn~\citep{Flatto1984,Flatto1985} & Exact (gen.\ func.) & No & Yes & Yes \\
Baccelli et al.~\citep{Baccelli1989} & Bounds & Partial & No & Yes \\
Varma--Makowski~\citep{Varma1994} & LH interp.\ (hom.) & Yes & Yes & No \\
Kemper--Mandjes~\citep{Kemper2012} & Bounds + asymptotics & Partial & No & Yes \\
Rizk et al.~\citep{Rizk2015} & Distributional bounds & No & No & Yes \\
Mohanty et al.~\citep{Mohanty2024} & Bounds ($(k,k)$ FJ) & Partial & No & Yes \\
\textbf{This work ($\Tul$)} & Bound interpolation & \textbf{Yes} & \textbf{Yes} & \textbf{Yes} \\
\textbf{This work ($\Tlh$)} & LH traffic interp.\ (0th order) & \textbf{Yes} & \textbf{Yes} & \textbf{Yes} \\
\textbf{This work ($\Tlhe$)} & LH traffic interp.\ (1st order) & \textbf{Yes} & \textbf{Yes} & \textbf{Yes} \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Recommendations}

For practical use:
\begin{itemize}
\item Use $\Tul$ when a guaranteed bound is required or when high heterogeneity
      ($r \geq 2$) is expected. It is simpler (no free parameters) and
      uniformly accurate.
\item Use $\Tlhe$ when $r \geq 2$ and the highest accuracy within the
      Reiman--Simon family is desired. It is strictly better than $\Tlh$
      for $r \geq 2$ and identical to it at $r = 1$.
\item Use $\Tlh$ (zero-order) when $r \approx 1.5$, where the first-order
      enhancement does not help and the simpler formula is preferable.
\item Use $\Tul$ and $\Tlhe$ together as a consistency check: agreement
      between them provides confidence in the estimate; significant
      disagreement signals a regime where further analysis
      (e.g., simulation or numerical inversion of the Flatto--Hahn generating
      function) is warranted.
\end{itemize}
